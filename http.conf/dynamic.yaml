http:
  routers:
    api:
      rule: "Host(`api.killianmahe.fr`)"
      entrypoints: websecure
      tls:
        certresolver: myresolver
      service: api-service

  service:
    api-service:
      loadBalancer:
        servers:
          - url: "http://localhost:8081"